<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre:</strong>
  <%= @pensum.nombre %>
</p>

<p>
  <strong>Date:</strong>
  <%= @pensum.date %>
</p>

<p>
  <strong>Universidad:</strong>
  <%= @pensum.carrera.universidad.nombre %>
</p>

<p>
  <strong>Carrera:</strong>
  <%= @pensum.carrera.nombre %>
</p>

<%= link_to 'Edit', edit_pensum_path(@pensum) %> |
<%= link_to 'Back', pensums_path %>


<div id='info_tables_asignaturas_id'>
  <h3>Asignaturas</h3>

  <table class='table table-hover'>
    <thead>
      <tr>
        <th>Clave</th>
        <th>Nombre</th>
        <th>Creditos</th>
        <th>Universidad</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @pensum.asignaturas.each do |asignatura| %>
        <tr>
          <td><%= asignatura.clave %></td>
          <td><%= asignatura.nombre %></td>
          <td><%= asignatura.creditos %></td>
          <td><%= asignatura.pensum.carrera.universidad.descripcion %> (<%= asignatura.pensum.carrera.universidad.nombre %>)</td>
          <td><%= link_to 'Ver', asignatura %></td>
          <td><%= link_to 'Editar', edit_asignatura_path(asignatura) %></td>
          <td><%= link_to 'Eliminar', asignatura, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>